export const metadata = {
  title: 'Send inn idé — VentureTech',
  description: 'Har du en idé til et teknologiselskap? Send inn søknaden din og VentureTech bygger selskapet sammen med deg.',
};

export default function SokLayout({ children }) {
  return children;
}
